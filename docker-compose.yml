services:
  php-8.2:
    image: oanhnn/php:8.2-cli-alpine
    volumes:
      - .:/app
    working_dir: /app
    command: >
      bash -c "apt-get update &&
               apt-get install -y libsqlite3-dev &&
               apt-get clean &&
               rm -rf /var/lib/apt/lists/* &&
               tail -f /dev/null"

  php-8.3:
    image: oanhnn/php:8.3-cli-alpine
    volumes:
      - .:/app
    working_dir: /app
    command: >
      bash -c "apt-get update &&
               apt-get install -y libsqlite3-dev &&
               apt-get clean &&
               rm -rf /var/lib/apt/lists/* &&
               tail -f /dev/null"

  php-8.4:
    image: oanhnn/php:8.4-cli-alpine
    volumes:
      - .:/app
    working_dir: /app
    command: >
      bash -c "apt-get update &&
               apt-get install -y libsqlite3-dev &&
               apt-get clean &&
               rm -rf /var/lib/apt/lists/* &&
               tail -f /dev/null"

  php-8.5:
    image: oanhnn/php:8.5-rc-cli-alpine
    volumes:
      - .:/app
    working_dir: /app
    command: >
      bash -c "apt-get update &&
               apt-get install -y libsqlite3-dev &&
               apt-get clean &&
               rm -rf /var/lib/apt/lists/* &&
               tail -f /dev/null"
